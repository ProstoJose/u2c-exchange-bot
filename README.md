# U2C Exchange Bot (Telegram) — GitHub + Railway

Бот считает курс и отправляет администратору:
- **🧮 Расчёт** (кнопка «Показать курс»)
- **🧾 Заявка** (кнопка «Отправить заявку»)

Также бот сохраняет **всех, кто нажал /start**, и администратор может выгрузить базу подписчиков в CSV.

## Валюты
- USD, EUR, UAH, RUB, USDT

## Источники курса
- **RUB↔EUR, USD↔RUB** — ЦБ РФ (официальный)
- **UAH↔EUR, UAH↔USD** — НБУ (официальный)
- **USDT↔EUR, USDT↔UAH, USDT↔USD** — Binance Spot (public ticker)

> Важно: для фиата (RUB/EUR/UAH/USD) это **официальные** курсы (обычно обновляются 1 раз в день),
а для USDT — биржевые цены Binance.

## Команды
- `/start` — начать, пользователь попадает в базу
- `/export_users` — **только админ**: выгрузка подписчиков (CSV)

---

# Запуск локально
1) Python 3.11+
2) Скопируй `.env.example` -> `.env` и заполни переменные.
3) Запусти Postgres локально или используй Railway Postgres.
4) Установи зависимости:

```bash
pip install -r requirements.txt
```

5) Запуск:

```bash
python -m bot.main
```

---

# Деплой на Railway (рекомендовано)

## 1) Создай репозиторий на GitHub
- Создай новый репо (например `u2c-exchange-bot`)
- Залей туда файлы этого проекта

## 2) Создай проект Railway
- Railway → New Project → Deploy from GitHub Repo
- Выбери репозиторий

## 3) Добавь Postgres
- В Railway внутри проекта → Add → **PostgreSQL**

## 4) Добавь переменные окружения
В сервисе бота (Variables):
- `BOT_TOKEN` — токен от @BotFather
- `ADMIN_ID` — Telegram user_id администратора (у тебя: **456774707**)
- `DATABASE_URL` — Railway обычно сам подставит после добавления Postgres (если нет — скопируй из Postgres plugin)
- `RATE_CACHE_TTL_SECONDS` — например `600`

## 5) Старт
Railway соберёт Dockerfile и запустит:
- `python -m bot.main`

---

# Как работает сценарий
1) Пользователь выбирает «Отдаёте» и «Получите»
2) Выбирает режим суммы: «Хочу обменять» или «Хочу получить»
3) Вводит сумму
4) Пишет *страна/город отправления* и *страна/город получения*
5) Жмёт «Показать курс» → бот:
   - считает курс
   - показывает пользователю итог
   - **отправляет админу уведомление «Расчёт»**
6) Пользователь пишет контакт текстом
7) Жмёт «Отправить заявку» → бот:
   - **отправляет админу уведомление «Заявка»**

---

# Примечания
- Округление:
  - **курс**: 2 знака
  - **«Отдам/получу»**: без копеек (целые)
- Хранение:
  - таблица `users` — подписчики (/start)
  - таблица `orders` — расчёты/заявки
